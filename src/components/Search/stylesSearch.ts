import styled from "@emotion/styled";
import { motion } from "framer-motion";

export const STitle = styled(motion.h1)`
  font-size: 32px;
  color: #fff;
`;

export const SFindContainer = styled(motion.form)`
  display: flex;
  justify-content: center;
  gap: 10px;
  width: 100%;
`;
